 this.router.events
      .pipe(filter(event => event instanceof NavigationEnd))
      .subscribe(() => {
        // Only validate if tabs exist
        if (this.tabs.length) {
          console.log('Tabs exist:', this.tabs);
          // You can add logic to clear tabs or update based on route
        }
      });
