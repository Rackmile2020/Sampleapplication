const dialogEl = componentRef.location.nativeElement as HTMLElement;

Array.from(document.body.children).forEach(child => {
  // Hide all siblings EXCEPT the dialog and backdrop
  if (child !== dialogEl && !child.classList.contains('modal-backdrop')) {
    (child as HTMLElement).setAttribute('aria-hidden', 'true');
  }
});
